<% provide(:title, 'All users') %>
<div class="container">
  <h2>ユーザー一覧</h2>
  <div class="row users-row">
    <ul class="users">
      <%= render @users %>
    </ul>
      <%= paginate @users %>
  </div>
</div>
