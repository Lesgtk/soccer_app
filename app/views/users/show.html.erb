<% provide(:title, @user.name) %>
<div class="container">
  <div class="main-title">
    プロフィール
  </div>

  <div class="row mypage-main">
    <div class="mypage-info col-lg-4 offset-lg-4">
    <div class="mypage-img">
      <% if @user.image? %>
          <%= image_tag @user.image.thumb50.url, class: "circle" %>
      <% else %>
          <%= image_tag "default.jpg", class: "circle" %>
      <% end %>
    </div>
    <div class="d-inline-block mypage-stats">
      ユーザー名:<%= @user.name %><br>
      投稿数: <%= @user.posts.count %>件<br>
      チェック数:<%= icon("far", "futbol", class: "soccer-icon") %>
      <%= @likes %>個<br>
      <%= render 'shared/stats' %>
    </div>
    <div class="mypage-follow">
      <%= render 'follow_form' if logged_in? %>
    </div>
  </div>
  </div>
  <div class="main-title">
    <%= icon("far", "futbol", class: "soccer-icon") %>
    投稿
    <%= icon("far", "futbol", class: "soccer-icon") %>
  </div>
  <div class="row mypage-content">
    <div class="mypage-feed col-md-8 offset-md-2">
      <% if @user.posts.any? %>
      <ol class="posts">
        <%= render @posts %>
      </ol>
      <% end %>
    </div>
  </div>
</div>
