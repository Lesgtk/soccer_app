<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "サッカーの技術、戦術、知識について投稿しよう" %>
  </div>
  <%= f.submit "投稿する", class: "btn btn-primary" %>
<% end %>
